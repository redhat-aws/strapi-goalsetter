'use strict';

/**
 * attribute-special-ability-taxonomy service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::attribute-special-ability-taxonomy.attribute-special-ability-taxonomy');
