'use strict';

/**
 *  attribute-special-ability-taxonomy controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::attribute-special-ability-taxonomy.attribute-special-ability-taxonomy');
