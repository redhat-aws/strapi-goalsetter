'use strict';

/**
 * attribute-special-ability-taxonomy router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::attribute-special-ability-taxonomy.attribute-special-ability-taxonomy');
