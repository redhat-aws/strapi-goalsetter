'use strict';

/**
 * gpt-assessment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::gpt-assessment.gpt-assessment');
