'use strict';

/**
 * attribute-taxonomy router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::attribute-taxonomy.attribute-taxonomy');
