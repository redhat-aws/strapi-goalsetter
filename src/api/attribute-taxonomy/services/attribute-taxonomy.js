'use strict';

/**
 * attribute-taxonomy service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::attribute-taxonomy.attribute-taxonomy');
